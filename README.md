#Image Renamer

This script is used to give images unique names based on their parent directories, so groups of images that come in separate directories can be flattened into one. 

###Use
- Pull down the script
- run an `npm install`
- run `node rename.js <ABSOLUTE DIRECTORY PATH>`
- hit enter